# `brmedia-ui` boundary

Historical package boundary for shared controls, lists and waveform widgets. The active shared shell/assets are under `server/public/shared`, while DJ waveform/runtime components are under `server/public/dj-mixer`.
