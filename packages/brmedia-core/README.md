# `brmedia-core` boundary

Historical package boundary for shared formats, presets, profiles and domain logic. Active implementations currently live primarily under `server`; this package directory is not the current runtime authority.
