# `brmedia-audio` boundary

Historical package boundary for shared FFmpeg, waveform and audio-pipeline adapters. Active implementations currently live under `server/src` and `server/public/dj-mixer`; this package directory is not the current runtime authority.
